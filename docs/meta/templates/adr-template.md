# ADR-NNN — <결정을 한 줄로>

> 작성 규칙은 [DOCUMENTATION.md](../DOCUMENTATION.md) §5를 따릅니다. 어디에 쓸지(§5.1·§5.3), 어느 필드를 채울지(§5.2), `Cross-repo impact`를 어떻게 신고할지(§5.4)가 거기 있습니다. cowork ADR과 저장소 ADR이 같은 템플릿을 쓰고, 다른 것은 채우는 필드뿐입니다.
> 이 문서에서 `>` 로 시작하는 안내 줄은 내용을 채운 뒤 지웁니다. 아래 머리말 블록은 남깁니다.

> Status: Proposed
> Date: YYYY-MM-DD
> Parent ADR: 없음
> Related ADR: <함께 읽어야 하는 결정. 없으면 이 줄을 지웁니다>
> Superseded By: <이 결정을 대체한 ADR. 없으면 이 줄을 지웁니다>
> Cross-repo impact: none
> 영향 저장소: <front · console · core · etl · mcp · infra · ios 중 해당하는 것>
> 승인자: 미승인

> `Cross-repo impact`는 저장소 ADR만, `영향 저장소`는 cowork ADR만 씁니다. 해당 없는 줄은 지웁니다.
> `Status`를 `Accepted`로 올리고 `승인자`를 채우는 것은 사람만 할 수 있습니다(원칙 VI). 그 전까지 이 문서는 제안입니다.

## 1. 배경

> 왜 지금 결정이 필요한지를 사실만으로 2~5문장. 결정 내용은 2절에 씁니다.
> 다른 문서가 정의한 개념은 다시 쓰지 않고 링크합니다(원칙 V).
> 예: 노지 스팟의 `trust_tier` 표시 방식이 저장소마다 다르게 구현돼 같은 스팟이 화면별로 다른 등급으로 보입니다.

## 2. 결정

> 무엇을 하기로 했는지 한 문단으로 단정해 적습니다.
> 정본 정의를 여기서 새로 만들지 않고 정본 문서를 링크합니다(원칙 V).

## 3. 검토한 대안

> 최소 한 개를 적습니다. "다른 안을 보지 않았다"면 그것 자체가 검토 부족입니다.

| 대안 | 장점 | 채택하지 않은 이유 |
|---|---|---|
| | | |

**복잡도 정당화**

> 더 단순한 대안이 있는데도 복잡한 쪽을 택했다면 그 이유를 반드시 적습니다(원칙 VII).
> 가장 단순한 안을 택했다면 "해당 없음"이라고 적습니다.
> 지금 요구사항이 요구하지 않는 일반화·확장 지점은 도입하지 않는 것이 기본값입니다.

## 4. 안전·합법성 영향

> 이 결정이 야영·취사·화기 가능 여부, 출입 통제, 보호구역 표시(원칙 I)나 출처·신뢰도 등급·확인 시점(원칙 II)에 닿는지 판정합니다.

영향: 없음 | 있음

> "없음"이어도 **왜 닿지 않는지** 한 줄을 적습니다. 판정만 남기면 검토했다는 착각만 만듭니다.
> "있음"이면 아래 세 줄을 채웁니다. 하나라도 비면 `Accepted`로 올리지 않습니다.

- 근거 없는 값이 '가능/허용'으로 노출되지 않음을 보이는 방법:
- 노출 게이트(`pipeline_status`)와 신뢰도 표시(`trust_tier`) 두 축의 분리가 유지되는지:
- 이 변경을 승인한 사람과 승인 시점:

## 5. 결과

> 이 결정이 만들어내는 후속 작업과 대가를 적습니다.

- 갱신해야 하는 정본 문서: <링크. 없으면 "없음">
- 의도적으로 남기는 기술 부채와 해소 조건: <원칙 VII — 기록되지 않은 부채는 허용하지 않습니다. 없으면 "없음">
- 뒤집는 결정: <원래 결정이 무엇이었고 왜 뒤집는지 함께 적습니다(원칙 V). 원본 ADR은 지우지 않고 `Status: Superseded`와 `Superseded By`만 바꿉니다. 없으면 "없음">
- 자식 ADR 검토 결과: <cowork ADR을 Superseded로 바꿀 때만. 이 ADR을 `Parent`로 가리키던 저장소 ADR이 여전히 유효한지 확인한 결과. 해당 없으면 이 줄을 지웁니다>
